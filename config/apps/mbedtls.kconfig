# Generated by tools/appctl menu - do not edit.
config APP_MBEDTLS
    bool "mbedtls: Mbed TLS 3.6.7, the TLS/HTTPS stack (kernel-net under CONFIG_TLS) (unavailable: requires kernel-net (M11 kernel network stack (CONFIG_NET)), posix-libc (M14 POSIX/libc layer))"
    default n
    help
        Mbed TLS 3.6.7, the TLS/HTTPS stack (kernel-net under CONFIG_TLS)
        unavailable until the required layer lands: requires kernel-net (M11 kernel network stack (CONFIG_NET)), posix-libc (M14 POSIX/libc layer); this symbol stays default n.
        version 3.6.7, license Apache-2.0, gpl=false.
