[package]
name = "kernel-riscv"
version = "0.0.4"
edition = "2021"
description = "fantuan kernel — RISC-V (M9.1)"

[dependencies]
fantuan-abi = { path = "../abi" }
kernel-core = { path = "../kernel-core" }

[build-dependencies]
cc = "1"
