/* fantuan adaptation shim: config-generated pfsync count (not upstream).
 * config(1) emits this from the kernel configuration. */
#ifndef FANTUAN_PFSYNC_H
#define FANTUAN_PFSYNC_H

#define NPFSYNC 0

#endif
