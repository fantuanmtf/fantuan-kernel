/* fantuan adaptation shim: sys/buf.h - the buffer cache is outside the M11
 * slice; uipc_socket2.c includes the header but uses no buffer services. */
#ifndef FANTUAN_SYS_BUF_H
#define FANTUAN_SYS_BUF_H
#endif
