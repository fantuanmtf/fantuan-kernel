/* fantuan adaptation shim: config-generated CARP count (not upstream).
 * config(1) emits this from the kernel configuration. */
#ifndef FANTUAN_CARP_H
#define FANTUAN_CARP_H

#define NCARP 0

#endif
