/* fantuan adaptation shim: config-generated driver counts (not upstream).
 * config(1) emits this from the kernel configuration; the fantuan slice
 * fixes the counts at zero until the ether path is imported in R4. */
#ifndef FANTUAN_ETHER_H
#define FANTUAN_ETHER_H

#define NETHER 0

#endif
